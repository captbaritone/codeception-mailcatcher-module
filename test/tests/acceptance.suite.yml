cass_name: NoGuy
modules:
    enabled:
        - MailCatcher
        # Disabled because it can't run on old versions of codeception
        #- Asserts
    config:
        MailCatcher:
            url: 'http://localhost'
            port: '1080'
